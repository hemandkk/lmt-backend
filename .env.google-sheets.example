# Copy relevant keys into your local .env

# Google Sheets — auto-append on lead create
GOOGLE_SHEETS_ENABLED=true
GOOGLE_SHEETS_SPREADSHEET_ID=your_spreadsheet_id_here
GOOGLE_SHEETS_WORKSHEET_NAME=Leads
GOOGLE_SERVICE_ACCOUNT_FILE=app/credentials/google-service-account.json
# Or paste JSON inline instead of a file:
# GOOGLE_SERVICE_ACCOUNT_JSON={"type":"service_account",...}
GOOGLE_SHEETS_MAX_RETRIES=3
GOOGLE_SHEETS_RETRY_BACKOFF_SECONDS=1.5

CORS_ORIGINS=https://crm-lm-frontend-9tny06osm-testercrm94-8474s-projects.vercel.app,http://localhost:3000



# Storage — local disk (/uploads) or s3 (S3 / Cloudflare R2)
# Use local for development; s3 for Render/production.
# Deletes via the API also remove the object from S3/R2 (free storage).
STORAGE_BACKEND=local
# STORAGE_BACKEND=s3
# S3_BUCKET=lmt-uploads
# S3_REGION=auto
# S3_ENDPOINT_URL=https://<ACCOUNT_ID>.r2.cloudflarestorage.com
# S3_ACCESS_KEY_ID=
# S3_SECRET_ACCESS_KEY=
# S3_PUBLIC_BASE_URL=https://files.yourdomain.com



# Used to build absolute document URLs in the sheet
APP_BASE_URL=http://localhost:8000
